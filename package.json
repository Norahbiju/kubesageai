{
  "name": "kubesage-ai",
  "private": true,
  "scripts": {
    "dev:web": "npm --prefix apps/web run dev",
    "dev:api": "cd services/api && uvicorn app.main:app --reload --port 8000"
  },
  "workspaces": [
    "apps/web"
  ]
}
